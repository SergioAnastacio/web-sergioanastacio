# Sergio Anastacio Website - Project Summary

## Purpose

`sergioanastacio.com` is the public website for Sergio Anastacio. The project is designed as a static-first personal and professional hub for:

- Personal brand and professional presence.
- Mobile app landing pages for App Store and Google Play.
- Support, privacy, and terms URLs required by app stores.
- Portfolio and service positioning for clients, recruiters, and collaborators.

The MVP intentionally avoids backend functionality. Contact uses `mailto:` links, and app/legal pages are generated statically.

## Current Status

- Framework: Astro.
- Hosting target: GitHub Pages.
- Deployment: GitHub Actions from `main`.
- Working branch: `dev`.
- Domain: `https://sergioanastacio.com`.
- DNS provider: Cloudflare.
- Email provider: Zoho Mail.
- SEO: complete technical SEO implemented with localized titles/descriptions, Open Graph, Twitter Cards, canonical URLs, `hreflang` alternate links, JSON-LD structured data (Person, WebSite, WebPage, BreadcrumbList), `robots.txt`, and prioritized sitemap.
- Sitemap: generated by `@astrojs/sitemap`.
- React support: enabled through `@astrojs/react`.
- Tailwind support: enabled through `@tailwindcss/vite`.
- Component library: no external component library is currently used.
- i18n: ES (default, at root) + EN (under `/en/`), configured via Astro i18n with `prefixDefaultLocale: false`.
- Dark mode: dark by default with a light toggle, applied before paint via an inline script.
- Typography: Fraunces (display serif), Inter (body), IBM Plex Mono (labels/status) via `@fontsource-variable`.

React is used for the home hero `HeroSignal` island. The theme toggle and mobile nav use lightweight vanilla inline scripts to stay static-first.

## Tech Stack

Runtime and build:

- Astro `7.x`
- React `19.x`
- Tailwind CSS `4.x`
- `@astrojs/sitemap`
- `@fontsource-variable/fraunces` (display)
- `@fontsource-variable/inter` (body)
- `@fontsource/ibm-plex-mono` (mono labels)
- GitHub Actions
- GitHub Pages

Development scripts:

```bash
npm install
npm run dev
npm run build
npm run preview
```

The build script disables Astro telemetry to avoid a Windows/libuv assertion seen locally after successful builds:

```bash
cross-env ASTRO_TELEMETRY_DISABLED=1 astro build
```

## Site Map

Active routes (Spanish, default at root):

```text
/
/apps
/apps/{app-slug}
/apps/{app-slug}/changelog
/portfolio
/portfolio/{case-study-slug}
/services
/about
/contact
/support
/support/{app-slug}
/privacy
/privacy/{app-slug}
/terms
/terms/{app-slug}
/legal
```

English mirrors the same structure under `/en/...`:

```text
/en/
/en/apps
/en/apps/{app-slug}
/en/apps/{app-slug}/changelog
/en/portfolio
/en/portfolio/{case-study-slug}
/en/services
/en/about
/en/contact
/en/support
/en/support/{app-slug}
/en/privacy
/en/privacy/{app-slug}
/en/terms
/en/terms/{app-slug}
/en/legal
```

Not active in the current MVP:

```text
/workana
```

Workana was intentionally removed for now.

## Current Pages

### Home

Main public landing page with:

- Personal brand hero.
- Apps overview.
- Services overview.
- Featured portfolio cards.
- Contact CTA.
- React island visual block: `HeroSignal`.

### Apps

The app system is data-driven from `src/data/apps.ts`.

Current placeholder app:

- `TaskForge`
- Status: `Coming Soon` (marked `concept: true` — displayed as "Concepto · No publicado")
- Platforms: iOS and Android

Generated app URLs (per locale):

```text
/apps/taskforge
/apps/taskforge/changelog
/privacy/taskforge
/support/taskforge
/terms/taskforge
```

### Portfolio

Portfolio is data-driven from `src/data/projects.ts`.

Current case studies:

- `Mobile Authentication Platform`
- `Cloud Delivery Automation`
- `AI Operations Workflows`

### Services

Services page currently covers:

- Mobile App Development
- Backend APIs with .NET
- Cloud & DevOps Automation
- AI Integration
- Software Architecture
- Technical Consulting

### Legal And Support

The following hubs exist:

- `/privacy`
- `/support`
- `/terms`
- `/legal`

Each app can have app-specific privacy, support, and terms pages generated from app data.

## Project Structure

```text
src/
├── components/
│   ├── AppCard.astro
│   ├── CTA.astro
│   ├── Footer.astro
│   ├── LanguageSwitcher.astro
│   ├── Navbar.astro
│   ├── ProjectCard.astro
│   ├── SectionReveal.astro
│   ├── ThemeToggle.astro
│   ├── pages/                  # locale-aware content components
│   │   ├── AboutContent.astro
│   │   ├── AppDetailContent.astro
│   │   ├── AppsContent.astro
│   │   ├── ChangelogContent.astro
│   │   ├── ContactContent.astro
│   │   ├── HomeContent.astro
│   │   ├── LegalContent.astro
│   │   ├── PortfolioContent.astro
│   │   ├── PortfolioDetailContent.astro
│   │   ├── PrivacyDetailContent.astro
│   │   ├── PrivacyIndexContent.astro
│   │   ├── ServicesContent.astro
│   │   ├── SupportDetailContent.astro
│   │   ├── SupportIndexContent.astro
│   │   ├── TermsDetailContent.astro
│   │   └── TermsIndexContent.astro
│   └── react/
│       └── HeroSignal.tsx
├── data/
│   ├── apps.ts
│   ├── projects.ts
│   └── site.ts
├── i18n/
│   ├── ui.ts                   # translation dictionary (ES + EN)
│   └── utils.ts                # getLangFromUrl, useTranslations, getLocalizedPath
├── layouts/
│   └── SiteLayout.astro
├── pages/
│   ├── index.astro
│   ├── 404.astro
│   ├── about.astro
│   ├── contact.astro
│   ├── legal.astro
│   ├── services.astro
│   ├── apps/
│   ├── portfolio/
│   ├── privacy/
│   ├── support/
│   ├── terms/
│   └── en/                     # English locale mirrors
│       ├── index.astro
│       ├── about.astro
│       ├── contact.astro
│       ├── legal.astro
│       ├── services.astro
│       ├── apps/
│       ├── portfolio/
│       ├── privacy/
│       ├── support/
│       └── terms/
└── styles/
    └── global.css
```

## Data Model

Centralized data lives in:

- `src/data/site.ts`: domain, emails, navigation, GitHub URL, avatar, mail helpers.
- `src/data/apps.ts`: app catalog, app details, FAQ, changelog, privacy, terms.
- `src/data/projects.ts`: portfolio/case study content.

This keeps app and portfolio expansion simple: add data first, and dynamic Astro routes generate the pages.

## Navigation

Header navigation:

```text
Apps
Portfolio
Services
Contact
```

Footer navigation:

```text
Products:
- Apps
- Support
- Privacy

Professional:
- Portfolio
- Services
- About
- Contact

Legal:
- Terms
- Legal
- GitHub
```

## Contact Emails

Published emails:

```text
contacto@sergioanastacio.com
soporte@sergioanastacio.com
apps@sergioanastacio.com
```

Current site contact method is `mailto:` pointing to Zoho-managed addresses. A future contact form or backend integration can be added only when there is a concrete need.

Email hosting/routing is implemented with Zoho Mail. The published addresses should remain configured in Zoho Admin Console and backed by the required DNS records in Cloudflare (MX, SPF, DKIM, and DMARC when enabled).

## Deployment

Deployment is configured through GitHub Actions:

```text
.github/workflows/deploy.yml
```

Publishing flow:

```text
dev -> pull request -> main -> GitHub Actions -> GitHub Pages
```

GitHub Pages should remain configured with:

```text
Source: GitHub Actions
Custom domain: sergioanastacio.com
```

The domain is preserved in:

```text
public/CNAME
```

## SEO And Indexing

The sitemap is generated automatically by Astro:

```text
/sitemap-index.xml
/sitemap-0.xml
```

Robots configuration:

```text
public/robots.txt
```

## Design Direction

Current visual direction (Moodboard B — Editorial Developer):

- Warm dark default (`#171513`) with a light toggle (`#fafaf9`).
- Fraunces italic serif for display headings; Inter for body; IBM Plex Mono for labels, status pills and tags.
- Single amber accent (`#fbbf24` dark / `#b45309` light) used sparingly for links, dots and key numbers.
- Editorial layout: generous whitespace, subtle dot-grid hero backdrop, inverted ink CTA bands.
- Status pills use semantic dots (green/amber/red). Concept apps show a distinct red badge.
- Static-first pages with limited client JavaScript (vanilla toggle/menu scripts, one React island).
- Scroll reveal animations via IntersectionObserver, respecting `prefers-reduced-motion`.

ReactBits-style components are possible later, but should be added selectively to avoid making the whole site JS-heavy.

## Validation Performed

Current implementation has been validated with:

```bash
npm run build
npm audit
```

Local route smoke tests were performed for:

```text
/
/apps
/apps/taskforge
/portfolio
/services
/contact
/legal
```

`/workana` is expected to be absent for now.

Desktop and mobile screenshots were reviewed locally with Playwright using Microsoft Edge.

## Near-Term Next Steps

Recommended next decisions:

1. Replace placeholder portfolio content with real public-safe case studies.
2. Migrate portfolio to Astro Content Collections to enable a blog/insights section.
3. Connect a CMS (Decap CMS recommended — Git-based, free) when there is editorial volume.
4. Create a dedicated 1200×630 Open Graph image for richer social sharing previews.
5. Translate app/project data content fully (currently UI chrome is bilingual; data is ES-first).
6. Merge `dev` into `main` when ready to publish the expanded site.
